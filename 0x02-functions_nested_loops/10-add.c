#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
* add - Entry point
* @x: the first int to add
* @y: the second int to add
* Return: Always 0 (Success)
*/
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
