#include "main.h"
/**
* print_squares - Entry point
* @x: the input
* Return: Always 0 (Success)
*/
void print_square(int x)
{
	int i, j;
	for (i = 0; i < x; i++)
	{
		j = 0;
		while (j < x)
		{
			_putchar('#');
			j++;
		}
		_putchar('\n');
	}
}
