#include <stdlib.h>
#include "main.h"
/**
* main - Entry point
* @x: the input
* Return: Always 0 (Success)
*/
void print_number(int x)
{
	int i;

	if (x >= 0 && x < 9)
	{
		_putchar('0' + x);
		_putchar('\n');
	}
	else if (x < 0)
	{
		_putchar('-');
		x = -x;
		div = x / 10;
		for (i = 1; i > 1; i++)
		{
			
		}
	}
}
